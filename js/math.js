/*
math equations
*/


// Range (m)(x)
function calculateDistance(x1, y1, x2, y2) {
    let a = x2 - x1;
    let b = y2 - y1;
    let distance = 10 * Math.sqrt(a**2 + b**2);
    return distance;
}

// Altitude (m)(y)

// Bearing (Deg.)

// Gravity (g)

// Velocity (v)